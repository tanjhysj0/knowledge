# Context Map

This repo uses a multi-context layout. Each context has its own `CONTEXT.md` and `docs/adr/` directory.

## Contexts

| Context | Location |
|---------|----------|
| (none yet) | — |

## How to add a new context

1. Create the context directory under `src/`
2. Add `CONTEXT.md` and `docs/adr/` inside it
3. Update this map with the new context entry
