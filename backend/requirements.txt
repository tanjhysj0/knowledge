fastapi==0.111.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.2.1
sqlalchemy==2.0.31
asyncpg==0.29.0
# #71：dense 向量存储迁至 PostgreSQL/pgvector（取代 Milvus）。
# psycopg2-binary 提供 VectorStoreService 的同步连接；pgvector 提供
# SQLAlchemy ``Vector`` 类型与余弦算子绑定。
pgvector>=0.3.0
psycopg2-binary>=2.9.9
anthropic>=0.39.0  # 0.39+ 兼容 httpx>=0.28（修复 #26：proxies kwarg 已被 httpx 0.28 移除）
openai>=2.0.0  # llm.py 默认 provider：AsyncOpenAI（容器构建暴露 requirements 缺此依赖）
pdfplumber==0.11.0
python-docx==1.1.0
python-dotenv==1.0.1
aiofiles==23.2.1
sse-starlette==2.0.0
sentence-transformers>=3.0.0  # 本地 bge-m3 embedding provider（#30）
torch>=2.0.0  # sentence-transformers 推理后端（#30）
numpy>=1.24.0  # sentence-transformers 依赖（#30）
langgraph>=1.0.0  # #66：Evidence Agent 证据循环状态机
jieba>=0.42.1  # #66：BM25 中文分词与实体抽取（实体/事件索引）
pytest==8.2.0
pytest-cov==4.1.0
